# 求职助 · AI 智能评估与动态面试说明


能力模型法（Competency Model）

核心思路：**把"算平均分"改成"门槛 + 增量"两级判定。**

```
第一级 · 硬性门槛（一票否决 Gate）
    逐条判定 JD 的硬性条件是否满足：
    学历 / 专业 / 必须技能 / 证书 / 年龄经验范围
    → 任一"必选项"不满足  ⇒  直接判定「不匹配/需补硬性」
    注：对应 SKLL.md 中"硬性要求 must-have"概念

第二级 · 软性增量（Soft competency）
    硬性全过才评估：软技能、业务理解、学习能力、可迁移经历……
    输出分项评分与证据、提升建议

最终给出 判定结果（可投递 / 补硬性 / 补软性）+ 匹配度百分比
```

**为什么更靠谱**：既守住底线（不会建议投一个学历硬伤不匹配的岗），又能体现应届生"无经验但有潜力"——无经验**不再作为硬伤扣分主因**，而是看可迁移经历与学习能力。

---

## 三、谁来执行"语义化"？—— 接 LLM（WorkBuddy 本地能力）

**让 LLM 从 JD 原文语义中动态拆解**该岗位真实的硬性/软性要求，再对照求职者背景判定。这正是工作台「AI 智能评估」页做的事。

工作流（文件导入模式）：

```
你在工作台：
  粘贴 JD → 点「生成评估指令」
        ↓ 复制
发给 WorkBuddy / Trae 等 AI（即本会话我）：
  AI 按能力模型法输出结构化 JSON（ai-eval-v1）
        ↓ 存成文件
你在工作台：
  点「导入并渲染」→ 选择该 JSON → 可视化（门槛表/软性评分/亮点风险/面试重点）
```

数据契约（AI 输出必须遵守，供工作台解析）：

```json
{
  "ver": "ai-eval-v1",
  "job": "安全售前工程师",
  "match_pct": 82,
  "gate": [ {"req":"硬性条件","type":"学历|专业|技能|证书|经验",
             "evidence":"你的证据","pass":true,"note":"说明"} ],
  "soft": [ {"dim":"软性能力","evidence":"证据","score":8,"note":"评价",
             "gap":"提升点","plan":"提升建议"} ],
  "verdict": {"ok":true,"label":"可投递","summary":"总评"},
  "highlights": ["亮点"], "risks": ["风险"],
  "interview_focus": ["面试重点"]
}
```

---

## 四、模拟面试：针对 JD + 求职者动态出题

**方案**：三层结构。

```
第 1 层  题型框架（固定，保证覆盖面）
        自我介绍 / 技术概念 / 简历深挖 / 行为面试 / 产品思维 / 案例面试
         ↓ 注入两个变量
第 2 层  AI 动态生成题目
        变量A = 该 JD 的核心要求与岗位关键词
        变量B = 求职者真实经历 + 已知短板（沟通表达慢等）
         → AI 结合 A、B 出一道"只对该生、对该岗"的题
         ↓
第 3 层  追问链 hint.depth
        每题附"为什么问这题" + "可能的追问方向"
```

**针对性示例**（而非通用模板）：
- 简历深挖 → 针对他的"同态加密毕设"，追问"服务端如何在不接触明文时推理？"（别人没有这段，背不了）。
- 案例面试 → "向完全不懂技术的客户解释为何要买安全产品"（售前真实场景）。
- 短板 → 一道结构化表达题，练"沟通组织慢"，附表达框架。
- AI 管培 → "把简历定制这个繁琐需求流程化为 agent 的思路"。

工作台用法：面试页 → 填 JD/岗位与侧重要求 → 「生成出题指令」复制给 AI → AI 返回 `ai-interview-v1` JSON → 「导入题目」→ 工作台按"🧠 AI 动态题"标签高亮渲染，含"为什么问这题"与参考要点。

数据契约：

```json
{
  "ver": "ai-interview-v1",
  "job": "安全售前工程师",
  "questions": [
    {"cat":"简历深挖","q":"题目","why":"为何针对此人/JD",
     "hint":"答题要点框架","depth":"可能的追问"}
  ]
}
```

---

## 五、两种评估如何分工

| | 快速评估（本地启发式） | AI 智能评估（能力模型法） |
|---|---|---|
| 引擎 | 前端关键词比对 | LLM 语义理解 |
| 速度 | 秒出、离线 | 需 AI 执行，一次几秒~几十秒 |
| 针对性 | 按 6 维命中 | 按 JD 实际要求动态归类 |
| 科学性 | 中（可做初筛） | 高（门槛+增量） |
| 场景 | 快速海投初筛 | 重点岗位投递前深度评估 |

**建议**：海投时用"快速评估"粗筛；对心仪岗位用"AI 智能评估"精判，再用其 `interview_focus` 作为动态面试的出题依据，形成"评估 → 补齐 → 针对性面试"的完整闭环。
